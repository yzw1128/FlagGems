# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch
import triton
import triton.language as tl

from flag_gems.runtime import torch_device_fn

logger = logging.getLogger(__name__)


@triton.jit
def arcsinh_kernel_(x_ptr, n_elements, BLOCK_SIZE: tl.constexpr):
    pid = tl.program_id(axis=0)
    block_start = pid * BLOCK_SIZE
    offsets = block_start + tl.arange(0, BLOCK_SIZE)
    mask = offsets < n_elements

    x = tl.load(x_ptr + offsets, mask=mask)
    x32 = x.to(tl.float32)
    x2 = x32 * x32
    tmp = tl.sqrt(x2 + 1.0)
    y32 = tl.log(x32 + tmp)
    y = y32.to(x.dtype)

    tl.store(x_ptr + offsets, y, mask=mask)


def arcsinh_(*args, **kwargs):
    logger.debug("GEMS ARCSINH_")
    if len(args) == 0:
        raise TypeError("arcsinh_ expected at least 1 argument (a Tensor)")
    x = args[0]
    if not isinstance(x, torch.Tensor):
        raise TypeError("arcsinh_ expected a torch.Tensor as the first argument")

    if (not x.is_contiguous()) or (not x.dtype.is_floating_point):
        torch.ops.aten.arcsinh_(x)
        return x

    n_elements = x.numel()
    grid = lambda meta: (triton.cdiv(n_elements, meta["BLOCK_SIZE"]),)
    with torch_device_fn.device(x.device):
        arcsinh_kernel_[grid](x, n_elements, BLOCK_SIZE=1024)
    return x
