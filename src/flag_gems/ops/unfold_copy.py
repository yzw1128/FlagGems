# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch
import triton
import triton.language as tl

logger = logging.getLogger(__name__)


@triton.jit
def _unfold_copy_kernel_2d(
    input_ptr,
    output_ptr,
    B: tl.constexpr,
    D: tl.constexpr,
    L: tl.constexpr,
    size: tl.constexpr,
    step: tl.constexpr,
    numel_out: tl.constexpr,
    BLOCK: tl.constexpr,
):
    """
    Optimized kernel for 2D input (B, D), unfolding dimension=1.
    Output: (B, L, size)
    output[b, l, k] = input[b, l*step + k]
    """
    pid = tl.program_id(0)
    offs = pid * BLOCK + tl.arange(0, BLOCK)
    mask = offs < numel_out

    # output shape: (B, L, size)
    # strides: (L*size, size, 1)
    k = offs % size
    tmp = offs // size
    l_idx = tmp % L
    b_idx = tmp // L

    # Compute input position
    pos = l_idx * step + k

    # Input is 2D with stride (D, 1)
    input_idx = b_idx * D + pos

    val = tl.load(input_ptr + input_idx, mask=mask, other=0.0)
    tl.store(output_ptr + offs, val, mask=mask)


@triton.jit
def _unfold_copy_kernel_3d_dim2(
    input_ptr,
    output_ptr,
    B: tl.constexpr,
    D: tl.constexpr,
    C: tl.constexpr,
    L: tl.constexpr,
    size: tl.constexpr,
    step: tl.constexpr,
    numel_out: tl.constexpr,
    BLOCK: tl.constexpr,
):
    """
    Kernel for 3D input (B, D, C), unfolding dimension=2.
    Output: (B, D, L, size)
    output[b, d, l, k] = input[b, d, l*step + k]
    """
    pid = tl.program_id(0)
    offs = pid * BLOCK + tl.arange(0, BLOCK)
    mask = offs < numel_out

    # output shape: (B, D, L, size)
    # strides: (D*L*size, L*size, size, 1)
    k = offs % size
    tmp = offs // size
    l_idx = tmp % L
    tmp2 = tmp // L
    d_idx = tmp2 % D
    b_idx = tmp2 // D

    # Compute input position
    pos = l_idx * step + k

    # Input is 3D with stride (D*C, C, 1)
    input_idx = b_idx * D * C + d_idx * C + pos

    val = tl.load(input_ptr + input_idx, mask=mask, other=0.0)
    tl.store(output_ptr + offs, val, mask=mask)


@triton.jit
def _unfold_copy_kernel_3d_dim1(
    input_ptr,
    output_ptr,
    B: tl.constexpr,
    D: tl.constexpr,
    C: tl.constexpr,
    L: tl.constexpr,
    size: tl.constexpr,
    step: tl.constexpr,
    numel_out: tl.constexpr,
    BLOCK: tl.constexpr,
):
    """
    Kernel for 3D input (B, D, C), unfolding dimension=1.
    Output: (B, L, C, size)
    output[b, l, c, k] = input[b, l*step + k, c]
    """
    pid = tl.program_id(0)
    offs = pid * BLOCK + tl.arange(0, BLOCK)
    mask = offs < numel_out

    # output shape: (B, L, C, size)
    # strides: (L*C*size, C*size, size, 1)
    k = offs % size
    tmp = offs // size
    c_idx = tmp % C
    tmp2 = tmp // C
    l_idx = tmp2 % L
    b_idx = tmp2 // L

    # Compute input position
    pos = l_idx * step + k

    # Input is 3D with stride (D*C, C, 1)
    input_idx = b_idx * D * C + pos * C + c_idx

    val = tl.load(input_ptr + input_idx, mask=mask, other=0.0)
    tl.store(output_ptr + offs, val, mask=mask)


def unfold_copy(
    input: torch.Tensor, dimension: int, size: int, step: int
) -> torch.Tensor:
    logger.debug("GEMS UNFOLD COPY")

    if step <= 0:
        raise ValueError("step must be > 0")

    ndim = input.ndim
    d = dimension % ndim
    D = input.shape[d]

    if size > D:
        raise ValueError("size must be <= dimension size")

    L = (D - size) // step + 1

    # Build output shape
    output_shape = input.shape[:d] + (L,) + input.shape[d + 1 :] + (size,)

    # Allocate output
    output = torch.empty(output_shape, dtype=input.dtype, device=input.device)

    numel_out = output.numel()
    # BLOCK size 128 provides good occupancy and throughput for common GPU architectures
    BLOCK = 128
    grid = lambda meta: (triton.cdiv(numel_out, meta["BLOCK"]),)

    # Dispatch to specialized kernels based on input shape and dimension
    if ndim == 2 and d == 1:
        # 2D input (B, D), unfold dim=1 -> output (B, L, size)
        B = input.shape[0]
        D = input.shape[1]
        _unfold_copy_kernel_2d[grid](
            input, output, B, D, L, size, step, numel_out, BLOCK=BLOCK
        )
    elif ndim == 3 and d == 2:
        # 3D input (B, D, C), unfold dim=2 -> output (B, D, L, size)
        B = input.shape[0]
        D = input.shape[1]
        C = input.shape[2]
        _unfold_copy_kernel_3d_dim2[grid](
            input, output, B, D, C, L, size, step, numel_out, BLOCK=BLOCK
        )
    elif ndim == 3 and d == 1:
        # 3D input (B, D, C), unfold dim=1 -> output (B, L, C, size)
        B = input.shape[0]
        D = input.shape[1]
        C = input.shape[2]
        _unfold_copy_kernel_3d_dim1[grid](
            input, output, B, D, C, L, size, step, numel_out, BLOCK=BLOCK
        )
    else:
        # For unsupported cases, raise an error
        raise NotImplementedError(
            f"unfold_copy is only implemented for 2D tensors with dim=1 "
            f"and 3D tensors with dim=1 or dim=2. Got ndim={ndim}, dim={d}"
        )

    return output
