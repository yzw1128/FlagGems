# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import triton
import triton.language as tl

from flag_gems.utils import pointwise_dynamic

logger = logging.getLogger(__name__)


@pointwise_dynamic(promotion_methods=[(0, "DEFAULT")])
@triton.jit
def atanh_func(x):
    # atanh(x) = 0.5 * ln((1 + x) / (1 - x))
    # Compute in float32 for better precision, then convert back
    x_fp32 = x.to(tl.float32)
    one = 1.0
    # Compute result: 0.5 * log((1 + x) / (1 - x))
    numerator = one + x_fp32
    denominator = one - x_fp32
    # For x outside (-1, 1), log of negative or zero gives NaN/inf naturally
    result = 0.5 * tl.math.log(numerator / denominator)
    return result.to(x.dtype)


def atanh(A):
    logger.debug("GEMS ATANH")
    return atanh_func(A)


def atanh_(A):
    logger.debug("GEMS ATANH_")
    atanh_func(A, out0=A)
    return A
